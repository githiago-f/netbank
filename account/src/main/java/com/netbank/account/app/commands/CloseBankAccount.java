package com.netbank.account.app.commands;

import com.netbank.common.commands.BaseCommand;

public class CloseBankAccount extends BaseCommand {
    public CloseBankAccount(String id) {
        super(id);
    }
}
